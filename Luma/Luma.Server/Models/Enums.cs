namespace Luma.Server.Models;

public enum UserRole
{
    Admin,
    Member,
    Viewer
}

public enum TaskStatus
{
    ToDo,
    InProgress,
    Done
}

public enum TaskPriority
{
    Low,
    Medium,
    High
}
